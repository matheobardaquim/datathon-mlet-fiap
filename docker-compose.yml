version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mapeia a pasta atual ('.') do seu PC para a pasta '/app' no contêiner.
      # Isso significa que o 'predictions.log' criado em /app aparecerá na sua pasta.
      - .:/app
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus_server
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'